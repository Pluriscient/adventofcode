use std::fs::read_to_string;

#[allow(dead_code)] // disable warning over dead code
mod days;

fn main() {
    println!("Hello, world!");
}

// #[cfg(test)]
mod test {
    #[test]
    fn run() {
        println!("running test");
    }
}